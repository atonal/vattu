---
- hosts: vattu
  gather_facts: no
  pre_tasks:
    - name: Wait 300 seconds for target connection to become reachable/usable
      remote_user: "{{ user }}"
      wait_for:
        port: 22
  roles:
    - user
    - spotify
